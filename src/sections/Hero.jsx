import React from 'react';

const Hero = () => {
  return (
    <section id="hero" className="section">
      <div className="container">
        <h2 style={{ color: 'var(--accent-primary)', marginBottom: '16px' }}>Hero Section Placeholder</h2>
        <p style={{ color: 'var(--text-secondary)' }}>Content for Hero will be implemented in future phases.</p>
      </div>
    </section>
  );
};

export default Hero;
